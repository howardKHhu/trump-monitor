# ── 方法一（推薦）：直接貼上 Bearer Token ──────────────────────────
# 在瀏覽器登入 Truth Social 後，開啟 DevTools → Network
# 隨意點一個對 truthsocial.com/api/* 的請求
# 在 Request Headers 找到 Authorization: Bearer xxxxxxx
# 把 xxxxxxx 貼到下面

TRUTH_ACCESS_TOKEN=

# ── 方法二：帳號密碼（有 OTP 可能失敗）──────────────────────────────
# TRUTH_EMAIL=your_email@example.com
# TRUTH_PASSWORD=your_password
